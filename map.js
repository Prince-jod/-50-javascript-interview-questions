//using map on array and def of parameter passed in an array 

const arr = [10, 20, 30];

arr.map((value, index, array) => {
    console.log(value);
    console.log(index);
    console.log(array);
});